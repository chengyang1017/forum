/// Framework-neutral description of a local image selected for a note.
final class LocalNoteImage {
  const LocalNoteImage({required this.path, required this.name});

  final String path;
  final String name;
}
